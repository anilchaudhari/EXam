package com.online.exam.model;

//This enum stores 'UsersAccountEnabled'
//AccountStatus	ENUM
//TRUE - account is created but, not active. User can't login and use it.
//FALSE - account is created and marked active. User can use it.
//Holding in database for certain backup or administrative purposes.
//----

//users account enabled enumeration
public enum UserAccountEnabled {
	 TRUE,
     FALSE
}
